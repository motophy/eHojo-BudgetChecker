"""
설정 상수 모듈

이 모듈은 엑셀 파일 처리에 필요한 모든 상수를 정의합니다.
- 예산서와 지출집행 파일의 컬럼 이름
- 엑셀 출력 시 각 컬럼의 너비

작성자: eHojo BudgetChecker Team
버전: 0.1.0
"""


class Constant:
    """
    애플리케이션에서 사용하는 상수들을 정의하는 클래스.

    이 클래스는 인스턴스를 생성하지 않고 클래스 변수로만 사용됩니다.
    예산서(Budget) 파일과 지출집행내역(Execution) 파일의 컬럼 정보를 포함합니다.
    """
    # ========== 예산서(Budget) 파일 관련 컬럼 ==========
    # 예산서에서 고유 항목을 식별하기 위한 키 컬럼들
    # 이 컬럼들의 조합으로 같은 예산 항목인지 판단합니다
    BUDGET_ITEM_COLUMNS = ('부서코드', '정책사업코드', '단위사업코드', '세부사업코드', '통계목')
    # 같은 예산 항목이 여러 행에 있을 때 병합할 컬럼들
    # 이 컬럼들의 값을 줄바꿈으로 결합하여 출력합니다
    BUDGET_JOIN_COLUMNS = ('의무/재량구분', '산출근거명', '산출근거식', '예산구분', '예산액')
    # 예산서 파일에서 출력할 전체 컬럼들 (총 28개)
    # 예산 항목의 모든 정보를 포함합니다
    BUDGET_COLUMNS = ("회계연도", "예산확정일자", "분야코드", "분야명", "부문코드", "부문명", "위원회코드", "위원회명", "정책사업코드", "정책사업명", "단위사업코드", "단위사업명", "회계코드", "회계명", "세부사업코드", "세부사업명", "자체보조구분", "자체보조구분명", "직접지원구분", "직접지원구분명", "사업형태", "사업형태구분명", "부서코드", "부서명", "편성목", "편성목명", "통계목", "통계목명")

    # ========== 지출집행내역(Execution) 파일 관련 컬럼 ==========
    # 지출집행내역에서 예산 항목을 식별하기 위한 키 컬럼들
    # BUDGET_ITEM_COLUMNS와 매칭되어 같은 예산 항목의 지출을 찾습니다
    EXECUTION_ITEM_COLUMNS = ('부서코드', '정책사업코드', '단위사업코드', '세부사업코드', '통계목코드')
    # 지출집행내역 파일에서 출력할 전체 컬럼들 (총 32개)
    # 지출 결의부터 지급까지의 전체 프로세스 정보를 포함합니다
    EXECUTION_COLUMNS = ("결의금액", "지급일자", "적요", "거래처명", "품의번호", "품의일자", "품의금액", "품의상태", "발의일자", "차액(품의액-원인행위액)", "원인번호", "원인행위일", "원인금액", "원인상태", "원인요청일", "원인구분", "결의일자", "결의상태", "결의요청일", "결의구분", "지급명령번호", "지급상태", "명령구분", "품의유형", "계약대장관리번호", "품의담당자", "품의승인자", "원인담당자", "원인결재자", "결의담당자", "지급담당자", "지급결재자")

    # ========== 엑셀 출력 설정 ==========
    # 엑셀 컬럼의 기본 너비 (0 = 자동 너비)
    DEFAULT_WIDTH = 0

    # 특정 컬럼의 너비를 지정합니다 (단위: 문자 수)
    # 가독성을 위해 컬럼 내용에 따라 적절한 너비를 설정합니다
    COLUMN_WIDTH = {
        '회계연도': 8.5,
        '정책사업명': 18,
        '단위사업명': 11,
        '세부사업명': 15,
        '부서명': 8.5,
        '편성목명': 8.5,
        '통계목': 8.5,
        '통계목명': 16,
        '산출근거명': 60,
        '산출근거식': 26,
        '예산액': 12,
        '사업별예산액': 13.5,
        '잔액': 13.5,
        '총지출금액': 13.5,
        '결의금액': 13.5,
        '지급일자': 10,
        '적요': 42,
        '거래처명': 15,
        '품의담당자': 15,
        '지급결재자': 15,
    }
